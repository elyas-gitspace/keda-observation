apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: consumer
  namespace: keda-observation
  labels:
    app: consumer
    release: prometheus
spec:
  selector:
    matchLabels:
      app: consumer
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
